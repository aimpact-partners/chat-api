/************
Processor: ts
************/

// index.ts
declare namespace ns_0 {
  interface IMetadata {
    answer: string;
    summary?: string;
    progress?: string;
    error: {
      code: number;
      text: string;
    };
  }
  interface ISendMessageResponse {
    status: boolean;
    error?: {
      code: number;
      text: string;
    };
    iterator?: AsyncIterable<{
      chunk?: string;
      metadata?: IMetadata;
    }>;
  }
  export class Agent {
    static sendMessage(chatId: string, params: any, uid: string): Promise<ISendMessageResponse>;
  }
  export {};
}


export import Agent = ns_0.Agent;

export declare const hmr: {on: (event: string, listener: any) => void, off: (event: string, listener: any) => void };