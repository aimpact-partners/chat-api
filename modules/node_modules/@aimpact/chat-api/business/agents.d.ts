/************
Processor: ts
************/

// index.ts
declare namespace ns_0 {
  interface IMetadata {
    answer: string;
    synthesis: string;
  }
  interface ISendMessageResponse {
    status: boolean;
    error?: string;
    iterator?: AsyncIterable<{
      chunk?: string;
      metadata?: IMetadata;
    }>;
  }
  export class Agents {
    static sendMessage(chatId: string, prompt: string): Promise<ISendMessageResponse>;
  }
  export {};
}


export import Agents = ns_0.Agents;

export declare const hmr: {on: (event: string, listener: any) => void, off: (event: string, listener: any) => void };